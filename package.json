{
  "name": "atlasachievement",
  "version": "4.0.0",
  "description": "Base mínima do AtlasAchievement durante a reconstrução controlada.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "lint": "node scripts/lint.js",
    "typecheck": "node scripts/typecheck.js",
    "test": "node --test tests/*.test.js",
    "test:production": "node scripts/smoke-production.js",
    "build": "npm run check:runtime && npm run lint && npm run typecheck && npm test",
    "predeploy": "npm run check:runtime",
    "check:runtime": "node -e \"const major=Number(process.versions.node.split('.')[0]); if(major!==20){console.warn('Aviso: produção usa Node 20.x; atual = '+process.versions.node)}\""
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^4.21.2"
  },
  "engines": {
    "node": "20.x",
    "npm": ">=10"
  }
}
